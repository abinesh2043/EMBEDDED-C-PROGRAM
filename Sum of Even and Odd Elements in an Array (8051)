#include <reg51.h>

void main() {
    unsigned char arr[6] = {1, 2, 3, 4, 5, 6};
    unsigned char even_sum = 0, odd_sum = 0, i;

    for (i = 0; i < 6; i++) {
        if (arr[i] % 2 == 0)
            even_sum += arr[i];
        else
            odd_sum += arr[i];
    }

    while (1);
}
